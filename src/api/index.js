import {
  getImage,
  getImageUrl,
  checkHealth
} from "./image-host";
import {
  nextProduct
} from "./product-catalog";

export const api = {
  getImage,
  getImageUrl,
  checkHealth,
  nextProduct
};
